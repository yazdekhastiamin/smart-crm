# دیپلوی روی لیارا (Liara.ir)

این پروژه — همان‌طور که برای Railway هم آماده شده بود — به‌صورت **یک سرویس
واحد** دیپلوی می‌شود: بک‌اند Express هم API را سرو می‌کند و هم فایل‌های
build شده‌ی فرانت‌اند را. یعنی در پایان یک لینک واحد (`https://<app-id>.liara.run`)
دارید که کل برنامه رویش کار می‌کند.

**نکته‌ی مهم:** همه‌ی تغییرات schema/Decimal/Prisma که قبلاً برای Railway
آماده شده بود (provider = postgresql، فیلدهای پولی Decimal، migration های
Postgres، Prisma Client Extension) کاملاً مستقل از PaaS هستند و بدون هیچ
تغییری همین‌جا هم کار می‌کنند. تنها چیزی که مخصوص هر PaaS است، فایل
پیکربندی (`railway.json` در برابر `liara.json`) و مراحل داشبورد است.

## پیش‌نیاز

- یک حساب کاربری در [console.liara.ir](https://console.liara.ir) با
  **موجودی کیف پول کافی** — بر خلاف Railway (که کارت بانکی بین‌المللی و
  پرداخت پسینی دارد)، لیارا پیش از استقرار به شارژ کیف پول نیاز دارد.
- ریپازیتوری روی GitHub (`yazdekhastiamin/smart-crm`) — همین الان هست.

## مراحل (در داشبورد لیارا)

### ۱. ساخت اپ (App)
۱. وارد [console.liara.ir](https://console.liara.ir) شوید → **برنامه جدید
   (New App)**.
۲. یک شناسه (App ID) انتخاب کنید — همین در آدرس نهایی هم استفاده می‌شود:
   `https://<app-id>.liara.run`.
۳. پلتفرم را **Node.js** انتخاب کنید.
۴. یک پلن/منبع سخت‌افزاری مناسب انتخاب کنید (برای این MVP یک پلن کوچک کافیست).
۵. اگر می‌خواهید اپ و دیتابیس در یک شبکه‌ی خصوصی (Private Network) باشند —
   پیشنهاد می‌شود — همان‌جا شبکه را انتخاب/بسازید (بخش ۲ همین راهنما).

### ۲. ساخت دیتابیس PostgreSQL
۱. از منوی اصلی → **دیتابیس‌ها (Databases) → دیتابیس جدید**.
۲. نوع را **PostgreSQL** انتخاب کنید، نسخه را مشخص کنید (۱۵ یا ۱۶ توصیه می‌شود).
۳. یک شناسه برای دیتابیس انتخاب کنید.
۴. **همان Private Network که برای اپ انتخاب کردید** را برای دیتابیس هم
   انتخاب کنید — این باعث می‌شود اپ بتواند از طریق آدرس داخلی (سریع‌تر و
   امن‌تر، بدون نیاز به باز بودن دسترسی عمومی) به دیتابیس وصل شود.
5. منابع سخت‌افزاری را انتخاب و دیتابیس را بسازید.

> اگر اپ و دیتابیس را در یک شبکه نساختید، می‌توانید از تنظیمات دیتابیس
> **Public Network** را فعال کنید و از آدرس عمومی استفاده کنید — ساده‌تر،
> اما امنیتش کمتر از اتصال داخلی است.

### ۳. گرفتن Connection String دیتابیس
برخلاف Railway، لیارا رشته‌ی اتصال را خودکار به اپ تزریق نمی‌کند — باید
دستی کپی و در متغیرهای محیطی اپ Paste کنید:

۱. وارد صفحه‌ی همان دیتابیس شوید → تب **Settings** (یا "اطلاعات اتصال" /
   Connection Info).
۲. اگر اپ و دیتابیس روی یک Private Network هستند، آدرس داخلی (چیزی شبیه
   `postgresql://root:<PASSWORD>@<db-id>:5432/postgres`) را کپی کنید. در
   غیر این‌صورت، از آدرس Public (بعد از فعال‌کردن دسترسی عمومی) استفاده کنید.
۳. پیشنهاد می‌شود یک دیتابیس جدید (نه دیتابیس پیش‌فرض `postgres`) بسازید و
   نام آن را در انتهای connection string جایگزین کنید — مثلاً `.../smart_crm`.

### ۴. تنظیم متغیرهای محیطی اپ
وارد اپی که در مرحله‌ی ۱ ساختید شوید → تب **Environment Variables** →
این‌ها را اضافه کنید:

| نام متغیر | مقدار |
|---|---|
| `DATABASE_URL` | همان connection string مرحله‌ی قبل |
| `COMPANY_NAME` | نام شرکت شما (روی پیش‌فاکتور چاپ می‌شود) |
| `PORT` | `4000` (باید با فیلد `port` در `liara.json` یکی باشد) |

> **مهم:** متغیرهای محیطی را از طریق داشبورد یا دستور `liara env set` تنظیم
> کنید — نه داخل `liara.json`. طبق مستندات خود لیارا، اگر بخش `envs` را در
> `liara.json` تعریف کنید، **تمام** متغیرهای قبلاً ذخیره‌شده در اپ پاک و
> جایگزین می‌شوند؛ به همین دلیل `liara.json` این پروژه عمداً بدون بخش `envs`
> نوشته شده تا هیچ‌وقت مقادیر حساس (مثل رمز دیتابیس) داخل ریپو/git قرار نگیرد.

### ۵. اتصال ریپازیتوری GitHub و دیپلوی
۱. داخل صفحه‌ی اپ → تب **Source** (یا "استقرار جدید" / Deploy).
۲. روش **GitHub** را انتخاب کنید و در صورت اولین‌بار، دسترسی لیارا به
   حساب/ریپازیتوری GitHub‌تان را تأیید کنید.
۳. ریپازیتوری `smart-crm` و برنچ `claude/mini-erp-crm-sales-mvp-8it2wl` را
   انتخاب کنید (یا قبلش آن را به `main` مرج کنید).
۴. لیارا فایل `liara.json` را در ریشه‌ی ریپو پیدا می‌کند و پورت (`4000`) و
   نسخه‌ی Node.js (`20`) را طبق آن تنظیم می‌کند.
۵. روی **استقرار (Deploy)** بزنید.

در لاگ‌های build/deploy باید این ترتیب را ببینید:
1. نصب وابستگی‌های ریشه (چیزی برای نصب ندارد)
2. اجرای اسکریپت `build` ریشه: نصب و build فرانت‌اند، سپس نصب بک‌اند
   (که `postinstall: prisma generate` را هم خودکار اجرا می‌کند)
3. اجرای اسکریپت `start` بک‌اند: `prisma migrate deploy` → seed پایه
   (idempotent؛ مراحل قیف فروش + یک کاربر مدیر دمو) → بالا آمدن سرور
4. health check روی `/api/health` موفق می‌شود

### ۶. گرفتن لینک نهایی
همان آدرس `https://<app-id>.liara.run` (شناسه‌ای که در مرحله‌ی ۱ انتخاب
کردید) — همان‌جا هم UI و هم API در دسترس است. اگر دامنه‌ی اختصاصی هم
می‌خواهید، از تب **Domains** همان اپ اضافه کنید.

### ۷. (اختیاری) داده‌ی نمونه برای دمو
مثل نسخه‌ی Railway، فقط seed پایه (مراحل قیف فروش + یک کاربر) خودکار روی
هر دیپلوی اجرا می‌شود. برای داده‌ی نمونه (مخاطب/معامله)، یک‌بار با
[Liara CLI](#بخش-۳-cli-و-امکان-دیپلوی-مستقیم-از-این-محیط) از روی سیستم
خودتان اجرا کنید:

```bash
liara shell --app=<app-id>
# داخل shell همان کانتینر:
npm run seed:open-deals --prefix backend
npm run seed:forecast-history --prefix backend
```

(یا معادلش را با `liara app:shell` / نام دقیق دستور shell در نسخه‌ی نصب‌شده‌ی CLI‌تان چک کنید.)

---

## بخش ۱: تغییرات لازم نسبت به آماده‌سازی Railway

خلاصه‌ی چک‌لیست سوال اول شما:

| مورد | Railway | لیارا |
|---|---|---|
| provider دیتابیس در schema.prisma | postgresql | **بدون تغییر** — همان postgresql |
| فیلدهای پولی Decimal + Prisma extension | انجام‌شده | **بدون تغییر** |
| migration های Postgres | انجام‌شده | **بدون تغییر** |
| فایل پیکربندی PaaS | `railway.json` | `liara.json` (جدید، اضافه شد) |
| تزریق خودکار DATABASE_URL | بله (`${{Postgres.DATABASE_URL}}`) | **خیر** — باید دستی کپی/paste شود |
| متغیر PORT | خودکار توسط Railway ست می‌شود | باید دستی `PORT=4000` ست شود و با `liara.json` هماهنگ باشد |
| build/start script (`package.json` ریشه) | همان که ساختیم | **بدون تغییر** — لیارا هم دقیقاً همین رفتار (`npm install` → `build` script → `start` script) را دارد |
| سرو یکپارچه‌ی فرانت‌اند از بک‌اند (`src/app.js`) | انجام‌شده | **بدون تغییر** |

تنها فایل جدید همین `liara.json` است:

```json
{
  "port": 4000,
  "node": {
    "version": "20"
  },
  "healthCheck": {
    "command": "CMD curl --fail http://localhost:4000/api/health || exit 1",
    "interval": 30,
    "timeout": 15,
    "retries": 2,
    "startPeriod": 10
  }
}
```

فیلدهای `app` و `platform` عمداً در این فایل نیستند — طبق مستندات لیارا،
در روش استقرار با GitHub این دو فیلد بی‌اثرند (چون پلتفرم و شناسه‌ی اپ از
قبل، هنگام ساخت اپ در کنسول، مشخص شده‌اند) و فقط برای استقرار مستقیم با
CLI کاربرد دارند (بخش بعد).

---

## بخش ۲ و ۳: CLI لیارا و امکان دیپلوی مستقیم از این محیط

بله — برخلاف Railway (که برای اتصال ریپو به OAuth نیاز داشت و از این
محیط headless قابل انجام نبود)، **دیپلوی لیارا از طریق CLI کاملاً بدون
مرورگر/OAuth ممکن است** و فقط به یک **API Token** نیاز دارد. اگر این توکن
را در اختیارم بگذارید، می‌توانم مستقیماً از همین‌جا:

1. با `liara app:create` یک اپ Node.js بسازم
2. با `liara network:create` یک شبکه‌ی خصوصی بسازم (اختیاری ولی توصیه‌شده)
3. با `liara db:create` یک دیتابیس PostgreSQL بسازم
4. connection string دیتابیس را با `liara env set DATABASE_URL=... --app=<app-id>` روی اپ ست کنم
5. با `liara deploy --app=<app-id> --api-token=<token>` مستقیماً کد این
   ریپو را (بدون نیاز به GitHub) build و دیپلوی کنم
6. سلامت اپ را با curl زدن به `/api/health` تأیید و لینک نهایی را بدهم

نحوه‌ی ساخت API Token: در کنسول لیارا → **تنظیمات حساب (Account Settings)
→ API Tokens → ساخت توکن جدید**.

**نکته‌ی مهم قبل از این کار:** مراحل بالا منابع واقعی و **هزینه‌بر** روی
حساب شما می‌سازند (اپ + دیتابیس، هر دو از موجودی کیف پول لیارا کسر
می‌شوند) — طبق رویه‌ی احتیاطی، این کار را بدون تأیید صریح شما شروع
نمی‌کنم. اگر مایلید کل فرآیند را من از همین‌جا انجام بدهم، کافیست:
- یک API Token بسازید و اینجا بفرستید، و
- تأیید کنید که پلن/منابع سخت‌افزاری پیش‌فرض (کوچک‌ترین پلن) برای اپ و
  دیتابیس مورد قبول است (یا مشخص کنید کدام پلن را می‌خواهید).

در غیر این صورت، همان مسیر داشبورد (بخش «مراحل» بالا) را دنبال کنید —
دقیقاً همان نتیجه را می‌دهد، فقط چند کلیک دستی بیشتر دارد.
