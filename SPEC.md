# مشخصات پروژه: مینی ERP برای SME ها — ماژول CRM و فروش (MVP)

## ۱. چشم‌انداز پروژه

یک مینی ERP سبک و مقرون‌به‌صرفه برای کسب‌وکارهای کوچک و متوسط (SME) که به‌جای پیاده‌سازی سنگین BPMS سنتی، از اتوماسیون مبتنی بر AI برای اجرای فرآیندهای اختصاصی هر شرکت استفاده می‌کند و به نرم‌افزارهای اصلی موجود (به‌خصوص نرم‌افزار مالی) متصل می‌شود، نه اینکه جایگزینشان کند.

## ۲. بازار هدف

* شرکت‌های کوچک و متوسط ایرانی
* شرکت‌هایی که از قبل یک نرم‌افزار مالی دارند (سپیدار، راهکاران، حسابفا و مشابه) اما فاقد ابزار ساختاریافته برای فروش/CRM هستند
* تیم‌های فروش کوچک (۲ تا ۲۰ نفره) که در حال حاضر با اکسل یا واتساپ کار می‌کنند

## ۳. محدوده MVP — ماژول CRM و فروش

### ویژگی‌های اصلی (باید باشد)

1. مدیریت مخاطبین و شرکت‌ها: ثبت مشتری، اطلاعات تماس، تاریخچه تعامل
2. قیف فروش (Sales Pipeline): مراحل قابل‌تنظیم (سرنخ → مذاکره → پیش‌فاکتور → بسته‌شده)
3. پیش‌فاکتور و فاکتور: صدور پیش‌فاکتور ساده، تبدیل به فاکتور
4. یادآوری و پیگیری: تسک و یادآور برای هر مخاطب/فرصت فروش
5. گزارش ساده: نرخ تبدیل، فروش ماهانه، عملکرد هر کارشناس فروش

### لایه اتصال مالی (Financial Adapter Layer)

* طراحی به‌صورت ماژولار: یک فرمت داده استاندارد داخلی (مشتری، آیتم فاکتور، مبلغ، مالیات)
* هر نرم‌افزار مالی (سپیدار/راهکاران/حسابفا/...) یک "adapter" جدا دارد که داده را به فرمت آن نرم‌افزار تبدیل می‌کند
* نسخه اول MVP: فقط خروجی Excel/CSV استاندارد (برای import دستی) — چون سریع‌ترین راه برای تست بازار بدون نیاز به API رسمی هر نرم‌افزار است
* نسخه بعدی: اتصال مستقیم API به نرم‌افزارهای پرکاربرد بر اساس تقاضای واقعی مشتریان اول

### فیچرهای هوشمند AI (بخش تمایز محصول)

#### ۳.۱ پیش‌بینی درآمد محتمل (Weighted Pipeline Forecast)

هر فرصت فروش دو مقدار دارد: ارزش ریالی و احتمال بسته‌شدن. ارزش برآوردی هر فرصت از ضرب این دو به‌دست می‌آید و جمع آن روی کل قیف، پیش‌بینی درآمد را می‌سازد.

```
ارزش برآوردی فرصت = ارزش ریالی × احتمال بسته‌شدن
پیش‌بینی کل قیف = مجموع ارزش برآوردی همه فرصت‌های باز
```

محاسبه احتمال بسته‌شدن (rule-based برای MVP، بعداً قابل تبدیل به مدل یادگرفته‌شده):

* پایه بر اساس مرحله قیف (نرخ تبدیل تاریخی هر مرحله — مقادیر اولیه قابل تنظیم):
   * سرنخ: ۱۰٪ | مذاکره: ۳۵٪ | پیش‌فاکتور ارسال‌شده: ۶۰٪ | چانه‌زنی نهایی: ۸۰٪
* تعدیل با سیگنال‌ها:
   * تازگی آخرین تعامل (هرچه قدیمی‌تر، احتمال کمتر)
   * سرعت پیشرفت بین مراحل (سریع‌تر = احتمال بیشتر)
   * لحن یادداشت‌ها/ایمیل‌ها (تشخیص تعهد یا تردید مشتری)
   * شباهت به فرصت‌های مشابه گذشته (اندازه شرکت، صنعت) در همان مرحله

نمایش: روی هر کارت فرصت رنگ/عدد ریسک + یک خط توضیح کوتاه (مثلاً «۱۴ روزه تماس نگرفتی»). بالای قیف، عدد کلی «پیش‌بینی درآمد این ماه/فصل». نام‌گذاری در UI: «پیش‌بینی درآمد محتمل» (بدون اصطلاح فنی/ریاضی).

#### ۳.۲ آلارم پیگیری (Follow-up Alerts)

لیست روزانه «امروز این‌ها را پیگیری کن» بر اساس ترکیب:

* تعداد روز از آخرین تعامل (آستانه بسته به مرحله قیف متفاوت است)
* رد شدن تاریخ قول‌داده‌شده
* افت امتیاز پیش‌بینی نسبت به هفته قبل

هر آیتم لیست باید یک دلیل کوتاه داشته باشد، نه فقط اسم مشتری.

#### ۳.۳ اولویت‌بندی لیدها (Best-to-Pursue Ranking)

رتبه‌بندی فرصت‌های باز بر اساس: امتیاز پیش‌بینی + شباهت به معاملات موفق قبلی (اندازه/صنعت/منبع لید) + ارزش ریالی. هدف: پاسخ به «کدام لید را الان پیگیری کنم؟»

#### ۳.۴ تحلیل الگوی موفقیت (Win-Pattern Analysis)

بررسی معاملات برد/باخت گذشته برای کشف الگو (مثلاً «فلان صنعت/اندازه شرکت نرخ تبدیل بالاتری دارد»). نکته مهم: تا رسیدن به حجم داده کافی (پیشنهاد: حداقل ۲۰-۳۰ معامله بسته‌شده)، نتایج باید با عبارت «روند اولیه نشان می‌دهد...» ارائه شوند، نه به‌عنوان یافته قطعی — برای جلوگیری از گمراه‌کنندگی به مدیر.

#### ۳.۵ داشبورد مدیر

تمرکز بر چند کارت بزرگ و بصری به‌جای جدول پر از عدد:

* پیش‌بینی درآمد (عدد بزرگ + روند نسبت به ماه قبل)
* قیف فروش بصری (فانل با عرض متناسب با تعداد/ارزش هر مرحله)
* «نیاز به توجه» — لیست کوتاه فرصت‌های پرریسک
* بهترین بخش مشتری (segment) بر اساس تحلیل الگو

### خارج از محدوده MVP (فعلاً نه)

* انبارداری و مدیریت موجودی
* منابع انسانی و حقوق‌ودستمزد
* گردش کار تایید چندسطحی پیچیده
* اپلیکیشن موبایل (فقط وب، ریسپانسیو)

## ۴. کاربران و نقش‌ها (نسخه اول ساده)

* مدیر/مالک: دسترسی کامل، گزارش‌ها
* کارشناس فروش: مدیریت مخاطبین و فرصت‌های خودش

## ۵. استک فنی پیشنهادی

* Frontend: React (ساده، با کمک AI قابل توسعه سریع)، ساخته‌شده به‌صورت PWA (Progressive Web App) از همان ابتدا تا نسخه موبایل (به‌خصوص آیفون) بدون نیاز به کدنویسی جدا از طریق «Add to Home Screen» به‌دست بیاید. اپ Native فقط در صورت نیاز به push notification قوی یا دسترسی عمیق‌تر سیستمی، در فاز بعد بررسی شود.
* Backend: Node.js/Express یا مشابه ساده
* Database: PostgreSQL یا SQLite برای MVP
* Deploy: یک سرویس ساده مثل Railway/Render برای شروع (بدون نیاز به DevOps پیچیده)
* لایه AI: فراخوانی API مدل زبانی (برای تحلیل لحن یادداشت‌ها و خلاصه‌سازی) + منطق rule-based داخلی برای امتیازدهی و آلارم‌ها (نیازی به مدل ML اختصاصی در MVP نیست)

### ۵.۱ فاز بعد: لایه اتوماسیون n8n (بعد از دموی اول)

تصمیم: n8n در MVP اول اضافه نمی‌شود تا سرعت رسیدن به دمو کم نشود. بعد از دموی موفق به مشتری اول، به‌عنوان لایه‌ی جدا برای موارد زیر self-hosted راه‌اندازی می‌شود:

* اتصال مالی واقعی (بخش لایه Adapter مالی) — به‌جای کدنویسی مستقیم برای هر نرم‌افزار مالی (پارسیان و بعداً بقیه)
* فرآیندهای سفارشی هر مشتری — قوانین اختصاصی هر SME بدون تغییر کد اصلی CRM
* آلارم‌ها و اعلان‌ها (بخش ۳.۲) از طریق تلگرام/واتساپ/ایمیل
* فراخوانی AI برای خلاصه‌سازی/تحلیل لحن بدون شلوغ‌کردن backend اصلی

اتصال به CRM اصلی: از طریق webhook — CRM هنگام رویدادهای مهم (تغییر مرحله deal، ثبت لید جدید) به n8n خبر می‌دهد؛ n8n workflow مربوطه را اجرا می‌کند.

## ۶. معیار موفقیت MVP

* ۳ تا ۵ شرکت واقعی SME که ۲ هفته از آن به‌جای اکسل استفاده کنند
* امکان صدور پیش‌فاکتور و خروجی گرفتن برای وارد کردن به نرم‌افزار مالی موجودشان بدون خطای دستی

## ۷. سوالات باز (باید قبل از شروع کدنویسی جواب داده شوند)

* [ ] آیا مشتری اول را از قبل شناسایی کرده‌ای برای تست MVP؟
* [ ] فرمت دقیق خروجی مالی (کدام ستون‌ها، کدام نرم‌افزار اولویت اول است) با یک نمونه واقعی از یک شرکت هدف بررسی شود
* [ ] چند کاربر هم‌زمان در نسخه اول باید پشتیبانی شود؟

این فایل مرجع دائمی پروژه است. قبل از هر session کدنویسی در Cursor، این فایل را به مدل ارجاع بده تا context کامل حفظ شود.
